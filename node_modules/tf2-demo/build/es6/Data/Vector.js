export class Vector {
    static areEqual(a, b) {
        return a.x === b.x && a.y === b.y && a.z === b.z;
    }
    constructor(x, y, z) {
        this.x = x;
        this.y = y;
        this.z = z;
    }
}
//# sourceMappingURL=Vector.js.map